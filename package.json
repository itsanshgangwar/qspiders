{
  "name": "night-coding-marathon",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf backend/dist && cp -r frontend/dist backend/",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18"
  }
}